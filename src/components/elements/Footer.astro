---
// Sin cambios en los imports
import Container from "../shared/Container.astro";
---
<div class=></div>
<footer class="relative bg-gradient-to-tr from-gray-100 to-gray-200 dark:from-gray-900 pt-28 rounded-t-3xl">
  <!-- decoraciones de fondo -->
  <div class="absolute right-0 top-0 h-full w-full flex justify-end">
    <div class="w-28 h-28 flex rounded-xl relative blur-2xl">
      <span class="absolute w-16 h-16 -top-1 -right-1 bg-green-500 rounded-md rotate-45"></span>
      <span class="absolute w-16 h-16 -bottom-1 -right-1 bg-[#FCDC58] rounded-md rotate-45"></span>
      <span class="absolute w-16 h-16 -bottom-1 -left-1 bg-primary rounded-md rotate-45"></span>
    </div>
  </div>

  <div class="absolute left-0 bottom-0 h-full w-full flex items-end">
    <div class="w-28 h-28 flex rounded-xl relative blur-2xl">
      <span class="absolute w-16 h-16 -top-1 -right-1 bg-green-500 rounded-md rotate-45"></span>
      <span class="absolute w-16 h-16 -bottom-1 -right-1 bg-[#FCDC58] rounded-md rotate-45"></span>
      <span class="absolute w-16 h-16 -bottom-1 -left-1 bg-primary rounded-md rotate-45"></span>
    </div>
  </div>

  <!-- Contenido -->
  <Container className="pb-8 relative">
    <span class="absolute top-1/2 left-1/2 -translate-y-1/2 -translate-x-1/2 blur-2xl opacity-20 w-24 h-16 sm:w-48 sm:h-36 rounded-full rotate-12 skew-x-6 bg-primary"></span>

    <div class="flex justify-center items-center text-center px-4 relative">
      <p class="text-xl sm:text-2xl md:text-3xl text-heading-1 font-semibold max-w-3xl animate-popFade transition duration-1000">
        “As a developer, every project is a part of you: bring your passion, creativity, and effort to it. Remember, you're not just writing code; you're leaving your mark on the digital world.” 🚀
      </p>
    </div>
  </Container>

  <!-- Línea inferior -->
  <div class="bg-gradient-to-tl from-box-bg py-2 relative">
    <Container>
      <div class="flex justify-between items-center gap-6 md:text-lg text-heading-3">
        <div>
          &copy; <span id="year"></span> Fernando Urroz. All rights reserved
        </div>
        <div>
          Made with 💻 by <a href="https://github.com/Fernando0312" target="_blank" class="font-semibold">Fernando Urroz Orozco</a>
        </div>
      </div>
    </Container>
  </div>
</footer>

<style>
  @keyframes popFade {
    0% {
      opacity: 0;
      transform: scale(0.95) translateY(10px);
      filter: brightness(0.8);
    }
    100% {
      opacity: 1;
      transform: scale(1) translateY(0);
      filter: brightness(1.2);
    }
  }

  .animate-popFade {
    animation: popFade 1s ease-out;
  }
</style>